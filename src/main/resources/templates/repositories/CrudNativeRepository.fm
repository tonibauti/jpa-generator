package ${CrudRepositoryPackage};

import ${BaseCrudNativeRepositoryPackage}.AbstractCrudNativeRepository;
import ${EntityPackage}.${Entity};
<#if isMultipleKey>
import ${EntityPackage}.${Key};
</#if>
import org.springframework.stereotype.Repository;
<#list importList as import>
import ${import};
</#list>


@Repository
public class ${CrudNativeRepository} extends AbstractCrudNativeRepository<${Entity}, ${Key}>
{
}

